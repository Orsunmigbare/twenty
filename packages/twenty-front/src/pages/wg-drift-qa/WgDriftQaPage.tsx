export const WgDriftQaPage = () => (
  <div data-testid="wg-drift-qa">
    <h1>Webgraph drift QA</h1>
    <p>A route added to the code so that monitoring can notice the repository moved.</p>
  </div>
);
